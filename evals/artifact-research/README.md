# artifact-research eval seeds

Model-routing seeds for canonical project artifact research. Deterministic indexing/filtering/freshness behavior remains in package tests; live model trials are opt-in.
